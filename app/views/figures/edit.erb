<h1>Please edit the details about this figure below:</h1>
<form method="POST" action="/figures/<%= @figure.id%>">
<input id="hidden" type="hidden" name="_method" value="PATCH">
	<label for="name">Figure Name</label>
	<input type="text" name="figure[name]" id="figure_name" value="<%= @figure.name %>">
	<br></br>

	<strong>Choose titles:</strong>
	<% Title.all.each do |title| %>
	<ul><label><%= title.name %></label>
		<input type="checkbox" id="title_<%=title.id%>" name="figure[title_ids][]" value="<%= title.id %>" <%='checked' if @figure.titles.include?(title) %>></input></ul>
	<%end%>
	<br></br>

	<strong><label>and/or, create a new title:</label></strong>
     	<input  type="text" name="title[name]" id="new_title"></input>
  <br></br>

	<strong>Choose landmarks:</strong>
	<% Landmark.all.each do |landmark| %>
	<ul><label><%= landmark.name %></label>
		<input type="checkbox" id="landmark_<%=landmark.id%>" name="figure[landmark_ids][]" value="<%= landmark.id %>" <%='checked' if @figure.landmarks.include?(landmark) %>></input></ul>
	<%end%>
	<br></br>

	<strong><label>and/or, create a new landmark:</label></strong>
    <input  type="text" name="landmark[name]" id="new_landmark" placeholder="name"></input>
    <input type="text" name="landmark[year_completed]" placeholder="year completed"></input>
  <br></br>

	<input type="submit" value="Edit Figure">
	
</form>